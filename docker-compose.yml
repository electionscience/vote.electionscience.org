version: "3.9"

services:
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    environment:
      - SECRET_KEY=abcd12345
    ports:
      - "8000:8000"
    depends_on:
      - db
    volumes:
      - ./:/code
